// Copy this file to app_secrets.dart and fill in your values.
// app_secrets.dart is gitignored — never commit real keys.

class AppSecrets {
  static const googlePlacesApiKey = 'YOUR_GOOGLE_PLACES_API_KEY';
}
